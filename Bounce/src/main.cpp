#include <iostream>

#include "Bounce.h"

int main(int argc, const char *argv[]) {

    // Create a new Bounce 
    Bounce bounce = Bounce();
    bounce.run();
    
    // ANSI C requires this!
    return 0;
}
